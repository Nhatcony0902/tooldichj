# Max video upload size in MB shown/enforced client-side before upload starts.
# Must mirror backend's MAX_UPLOAD_MB (translation.controller.ts) — the backend
# value is authoritative since multer enforces it. NEXT_PUBLIC_* vars are
# inlined at build time, so rebuild the frontend after changing this value.
NEXT_PUBLIC_MAX_UPLOAD_MB=100
